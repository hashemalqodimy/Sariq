name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Prepare Environment and Keystore
        run: |
          touch .env
          if [ -f debug.keystore.base64 ]; then
            base64 -d debug.keystore.base64 > debug.keystore 2>/dev/null || true
          fi
          if [ ! -s debug.keystore ]; then
            echo "Generating debug.keystore..."
            keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          fi

      - name: Build Debug APK
        run: gradle assembleDebug --stacktrace

      - name: Prepare APK File
        run: |
          cp app/build/outputs/apk/debug/*.apk app/build/outputs/apk/debug/aman-phone.apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aman-phone-debug-apk
          path: app/build/outputs/apk/debug/aman-phone.apk

      - name: Release APK on GitHub
        uses: softprops/action-gh-release@v2
        continue-on-error: true
        with:
          tag_name: v1.0.0
          name: "Aman Phone - أمان فون v1.0"
          body: |
            ### 📱 تطبيق أمان فون (Aman Phone) - النسخة الجاهزة للتثبيت
            قم بتنزيل ملف **`aman-phone.apk`** المرفق بالأسفل وتثبيته مباشرة على هاتفك.
          files: app/build/outputs/apk/debug/aman-phone.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
